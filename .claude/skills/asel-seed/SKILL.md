---
name: asel-seed
description: Generate seed data; 'seed', 'test verisi', 'veri yükle'
context: fork
agent: asel-seed-generator
---

Run seed data generation as described in your agent definition against the project at the current working directory. Read `{{paths.routemap}}` and `docs/ARCHITECTURE.md` first. Create and run the seed script, verify screens/queries show meaningful data, and return a summary of what was seeded.
